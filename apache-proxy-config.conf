# APACHE PROXY CONFIGURATION FOR BACKEND API
# Add this to your Apache VirtualHost configuration

<VirtualHost *:80>
    ServerName nusantaragroup.co
    ServerAlias www.nusantaragroup.co
    
    # Proxy frontend to Docker container
    ProxyPreserveHost On
    ProxyRequests Off
    
    # Proxy /api to backend Docker container port 5000 (MUST BE FIRST!)
    ProxyPass /api http://172.19.0.3:5000/api
    ProxyPassReverse /api http://172.19.0.3:5000/api
    
    # Proxy root to frontend Docker container port 3000 (AFTER /api)
    ProxyPass / http://172.19.0.4:3000/
    ProxyPassReverse / http://172.19.0.4:3000/
    
    # Enable CORS headers for API
    <Location "/api">
        Header always set Access-Control-Allow-Origin "*"
        Header always set Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS"
        Header always set Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With"
        
        # Handle preflight OPTIONS requests
        RewriteEngine On
        RewriteCond %{REQUEST_METHOD} OPTIONS
        RewriteRule ^(.*)$ $1 [R=200,L]
    </Location>
    
    # Security headers
    Header always set X-Frame-Options DENY
    Header always set X-Content-Type-Options nosniff
    Header always set Referrer-Policy strict-origin-when-cross-origin
    Header always set X-XSS-Protection "1; mode=block"
    
    # Cache control for static assets
    <LocationMatch "\.(css|js|png|jpg|jpeg|gif|ico|svg)$">
        Header always set Cache-Control "public, max-age=31536000"
    </LocationMatch>
    
    # Logging
    ErrorLog ${APACHE_LOG_DIR}/nusantara_error.log
    CustomLog ${APACHE_LOG_DIR}/nusantara_access.log combined
</VirtualHost>

<VirtualHost *:443>
    ServerName nusantaragroup.co
    ServerAlias www.nusantaragroup.co
    
    # SSL Configuration
    SSLEngine on
    SSLCertificateFile /etc/letsencrypt/live/nusantaragroup.co/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/nusantaragroup.co/privkey.pem
    
    # Proxy frontend to Docker container
    ProxyPreserveHost On
    ProxyRequests Off
    
    # Proxy /api to backend Docker container port 5000 (MUST BE FIRST!)
    ProxyPass /api http://172.19.0.3:5000/api
    ProxyPassReverse /api http://172.19.0.3:5000/api
    
    # Proxy root to frontend Docker container port 3000 (AFTER /api)
    ProxyPass / http://172.19.0.4:3000/
    ProxyPassReverse / http://172.19.0.4:3000/
    
    # Enable CORS headers for API
    <Location "/api">
        Header always set Access-Control-Allow-Origin "*"
        Header always set Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS"
        Header always set Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With"
        
        # Handle preflight OPTIONS requests
        RewriteEngine On
        RewriteCond %{REQUEST_METHOD} OPTIONS
        RewriteRule ^(.*)$ $1 [R=200,L]
    </Location>
    
    # Security headers
    Header always set X-Frame-Options DENY
    Header always set X-Content-Type-Options nosniff
    Header always set Referrer-Policy strict-origin-when-cross-origin
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
    Header always set X-XSS-Protection "1; mode=block"
    
    # Cache control for static assets
    <LocationMatch "\.(css|js|png|jpg|jpeg|gif|ico|svg)$">
        Header always set Cache-Control "public, max-age=31536000"
    </LocationMatch>
    
    # Logging
    ErrorLog ${APACHE_LOG_DIR}/nusantara_ssl_error.log
    CustomLog ${APACHE_LOG_DIR}/nusantara_ssl_access.log combined
</VirtualHost>

# Required Apache modules:
# a2enmod proxy
# a2enmod proxy_http
# a2enmod headers
# a2enmod rewrite
# a2enmod ssl
