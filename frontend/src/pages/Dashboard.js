import React from 'react';

/**
 * Dashboard.js - Legacy wrapper for backward compatibility
 * This thin wrapper ensures existing imports continue to work
 * while the code is now better organized in the Dashboard folder
 */
import DashboardPage from './Dashboard/index';

export default DashboardPage;