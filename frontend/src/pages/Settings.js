import React from 'react';

/**
 * Settings.js - Legacy wrapper for backward compatibility
 * This thin wrapper ensures existing imports continue to work
 * while the code is now better organized in the Settings folder
 */
import SettingsPage from './Settings/index';

export default SettingsPage;