import React from 'react';

/**
 * Tax.js - Legacy wrapper for backward compatibility
 * This thin wrapper ensures existing imports continue to work
 * while the code is now better organized in the Tax folder
 */
import TaxPage from './Tax/index';

export default TaxPage;